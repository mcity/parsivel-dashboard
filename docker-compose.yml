services:
  backend:
    build: ./backend
    image: parsivel-backend:dev
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    command: gunicorn --bind 0.0.0.0:8000 --workers 2 --reload wsgi:app
    restart: unless-stopped
